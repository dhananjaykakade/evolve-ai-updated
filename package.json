{
  "name": "evolve ai",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "packageManager": "pnpm@10.12.3",
  "workspaces": [
    "backend-service/*"
  ],
  "scripts": {
    "seed": "pnpm --filter teacher-service seed && pnpm --filter auth-service seed ",
    "seed:all": "pnpm --filter auth-service migrate && pnpm --filter auth-service seed && pnpm --filter teacher-service seed && pnpm --filter student-service seed",
    "db:up": "cd database && docker compose --env-file .env up -d",
    "db:down": "cd database && docker compose down",
    "seed:orchestrated": "node scripts/seed-all.mjs",
    "dev": "kill-port 8001 && concurrently \"pnpm --filter api-gateway dev\" \"pnpm --filter auth-service dev\" \"pnpm --filter teacher-service dev\" \"pnpm --filter student-service dev\" \"pnpm --filter notification-service dev\" \"npm --prefix Evolve-Student run dev\" \"npm --prefix Evolve-main run dev\" \"npm --prefix Evolve-Teacher run dev\" \"npm --prefix Evolve-Exam run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "gitignore": "^0.7.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.9"
  }
}
